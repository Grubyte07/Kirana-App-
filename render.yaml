services:
  - type: web
    name: kirana-profit-manager
    runtime: node
    plan: free
    buildCommand: chmod +x build.sh && bash build.sh
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
